apiVersion: 1

providers:
  - name: 'MLX Monitoring Dashboards'
    orgId: 1
    folder: 'MLX Server'
    type: file
    disableDeletion: false  # Allows deletion via API/filesystem
    updateIntervalSeconds: 30  # Reduced frequency to minimize UI conflicts
    allowUiUpdates: true  # Allows editing in UI
    options:
      path: data/grafana-dashboards